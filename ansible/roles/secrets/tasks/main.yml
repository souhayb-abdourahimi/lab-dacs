---
# Génère /etc/lab-alertes.conf à partir des secrets du coffre Vault
- name: Déployer le fichier de secrets des alertes
  ansible.builtin.template:
    src: lab-alertes.conf.j2
    dest: /etc/lab-alertes.conf
    owner: root
    group: root
    mode: "0600"
